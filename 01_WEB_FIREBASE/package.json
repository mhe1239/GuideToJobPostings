{
  "name": "kangnam-notice-guide-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/generate-firebase-config.mjs && node scripts/generate-admin-config.mjs",
    "deploy": "npm run build && firebase deploy --only hosting --project guide-to-job-postings"
  }
}
